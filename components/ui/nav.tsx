import { styled } from "stitches.config";

const Nav = styled("nav", {
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  padding: "$4",
  width: "100%",
  minHeight: "100px",
  maxWidth: "$xlContainer",
  my: 0,
  mx: "auto"
});

export default Nav;
